#include<stdio.h>
int main()
{
    int r,c,sum=0,n;
    int arr[100][100];
    scanf("%d",&n);
for(r=0;r<n;r++)
    {
        for(c=0;c<n;c++)
        {
            scanf("%d",&arr[r][c]);
             if(r==c)
              {
            sum = sum + arr[r][c];
            }
        }
    }
